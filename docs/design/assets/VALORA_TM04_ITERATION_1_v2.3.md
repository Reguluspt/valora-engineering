# VALORA TM04 — Preview / Test fill dữ liệu Word — Iteration 1

**Trạng thái:** `PROMOTED TO BASELINE AUTHORITY`

Iteration này đã được người dùng duyệt và **chốt baseline**. Authority hiện hành được ghi tại:

[`VALORA_TM04_BASELINE_v2.3.md`](./VALORA_TM04_BASELINE_v2.3.md)

Các điểm visual/interaction của Iteration 1 được giữ làm nguồn truy vết lịch sử:

- Valora shell + Fluent 2, desktop-first;
- step rail `Upload file → Mapping bảng danh mục → Mapping thông tin chung → Preview / Test fill`;
- chọn bộ dữ liệu test và tùy chọn highlight mapping;
- vùng dữ liệu test có ngữ cảnh `Thông tin chung` / `Bảng danh mục`;
- preview trực tiếp tài liệu Word đã fill với zoom/page navigation/full-screen;
- validation text dài/tràn ô, format tiền/ngày, bảng lặp, footer/ngắt trang;
- CTA `Quay lại chỉnh mapping`, `Lưu template/Lưu bản nháp`, `Lưu và đặt sẵn sàng sử dụng`;
- `Sẵn sàng sử dụng` chỉ khi không còn lỗi blocking và người dùng thực hiện thao tác explicit.

Không dùng semantics Excel như sheet/cell/range trong TM04. Dữ liệu test chỉ phục vụ kiểm thử template và không trở thành dữ liệu hồ sơ chính thức.
